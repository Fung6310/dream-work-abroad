PORT=4100
WEB_ORIGIN=http://localhost:3100
ADMIN_ORIGIN=http://localhost:3101
ADMIN_PASSWORD=admin123

# Leave unset for local dev — the API falls back to local JSON files
# (apps/api/data.*.json) automatically. Set this to a Postgres connection
# string (e.g. from Supabase or Neon's free tier) to use a real database
# instead — required before deploying. See docs/DEPLOYMENT.md.
DATABASE_URL=
